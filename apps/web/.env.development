# Leave unset so the browser calls same-origin `/v1/...` and Vite proxies to FastAPI.
# That avoids CORS and “Failed to fetch” when you open Studio via LAN IP or non-listed origins.
# To talk to the API directly, set e.g. VITE_API_BASE_URL=http://127.0.0.1:8000
# VITE_API_BASE_URL=
